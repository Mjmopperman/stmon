Write only a single Python function called create_relationships(tables, endpoint, secret) that:
- Accepts a list of parsed YAML table definitions, a Hasura endpoint URL, 
  and an admin secret string
- For each object_relationship sends a pg_create_object_relationship POST request
  where foreign_key_constraint_on is a plain string column name only
- For each array_relationship sends a pg_create_array_relationship POST request
  where foreign_key_constraint_on is an object containing column and table
- Sets x-hasura-admin-secret header from secret
- Prints success or error per relationship without aborting on error

Output only the function — no imports, no main block, no explanations, 
no markdown fences.
Append to output/hasura_apply.py