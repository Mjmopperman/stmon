Write only a Python import block for a script that uses psycopg2, 
dotenv, os, pathlib, and yaml.
Output only code — no explanations, no markdown fences, no comments.
Append to output/pg_apply.py or create it if it does not exist