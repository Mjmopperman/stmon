Write only a single Python function called create_tables(conn, tables) that:
- Accepts a psycopg2 connection and a list of parsed YAML table definitions
- For each table generates and executes a CREATE TABLE IF NOT EXISTS statement
- Uses PostgreSQL syntax: INTEGER GENERATED ALWAYS AS IDENTITY, TEXT, TIMESTAMPTZ
- Prints success or error per table without aborting on error

Output only the function — no imports, no main block, no explanations, 
no markdown fences.
Append to output/pg_apply.py