Write only a single Python function called create_indexes(conn, tables) that:
- Accepts a psycopg2 connection and a list of parsed YAML table definitions
- For each index entry in each table generates and executes a 
  CREATE INDEX IF NOT EXISTS statement
- Prints success or error per index without aborting on error

Output only the function — no imports, no main block, no explanations, 
no markdown fences.
Append to output/pg_apply.py